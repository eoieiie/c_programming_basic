#include <stdio.h>

//if + 반복 = while

int main(void)
{

    // while ((c = getchar()) != EOF) 
    // {
    //     if (c >= 'a' && c <= 'z')
    //         ++lowercase_letter_cnt;
    //     ++total_cnt;
    // }

    //한 글자씩 읽어와서 소문자 여부를 판단 후 개수 증가
}